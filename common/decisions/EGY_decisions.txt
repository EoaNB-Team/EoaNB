political_actions = {

	#EGY annex south sudan
	eoanb_decision_EGY_take_south_sudan = {

		icon = generic_political_discourse

		available = {
			any_state = {
				is_owned_and_controlled_by = FROM
				is_claimed_by = ROOT
				any_neighbor_state = {
					is_fully_controlled_by = ROOT
				}
			}
			tag = EGY
		}
		allowed = { tag = EGY }
		visible = {
			TAG = EGY
			NOT = { has_war_with = FROM }
			FROM={
				any_owned_state = {
					is_claimed_by = ROOT
					any_neighbor_state = {
						is_fully_controlled_by = ROOT
					}
					OR = {
						state = 832
						state = 833
						state = 834
						state = 845
					}
				}
			}
			date < 1900.01.01
		}

		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
			}
			NOT = {
				is_subject_of = ROOT
			}
		}

		fire_only_once = yes

		cost = 25

		complete_effect = {
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
		}

		ai_will_do = {
			base = 1
		}
	}
}

afr_suez_construction = {

	#Dedicate Military Forces as Workers
	EGY_dedicate_military_workers = {
		icon = generic_construction

		allowed = { EGY_suez_participant_tags = yes }

		available = {
			has_manpower > 999
		}

		visible = {
			has_country_flag = suez_constructor_flag
			NOT = { has_global_flag = SUEZ_CANAL_BUILT }
			EGY = { exists = yes }
			check_variable = {
				var = EGY.suez_inprogress_total
				value = EGY.suez_progress_max
				compare = less_than
			}
		}

		cost = 0

		days_remove = 240
		days_re_enable = 10

		cancel_trigger = {
			check_variable = {
				var = EGY.suez_progress
				value = EGY.suez_progress_max
				compare = greater_than_or_equals
			}
		}

		modifier = {
			political_power_gain = -0.05
		}

		complete_effect = {
			add_manpower = -1000
			set_temp_variable = { suez_step = 1 }
			EGY_suez_begin_work = yes
			custom_effect_tooltip = EGY_suez_construct_1
		}

		remove_effect = {
			set_temp_variable = { suez_step = 1 }
			EGY_suez_finish_work = yes
			add_manpower = 900
		}

		cancel_effect = {
			set_temp_variable = { suez_step = 1 }
			EGY_suez_abort_work = yes
			add_manpower = 1000
		}

		ai_will_do = {
			base = 1000
			modifier = {
				factor = 0
				has_manpower < 5000
			}
			modifier = {
				factor = 0.1
				NOT = {
					OR = {
						has_government = autocracy
						has_government = chauvinist_populism
						has_government = radical_democracy
					}
				}
			}
		}
	}

	EGY_corvee_workers_alexandria = {
		icon = generic_construction
		fixed_random_seed = no

		allowed = { TAG = EGY }

		available = {
			check_variable = {
				var = EGY.suez_inprogress_total
				value = EGY.suez_progress_max
				compare = less_than
			}
			447 = {
				state_population > 9999
			}
			has_stability > 0.20
			NOT = { has_country_flag = EGY_corvee_suez_disabled }
		}

		visible = {
			NOT = { has_country_flag = EGY_corvee_suez_disabled }
			has_country_flag = suez_constructor_flag
			NOT = { has_global_flag = SUEZ_CANAL_BUILT }
			owns_state = 447
		}

		cost = 0
		ai_will_do = { factor = 100 }

		modifier = {
			political_power_gain = -0.02
			stability_factor = -0.025
		}

		days_remove = 240

		cancel_trigger = {
			check_variable = {
				var = EGY.suez_progress
				value = EGY.suez_progress_max
				compare = greater_than_or_equals
			}
		}

		complete_effect = {
			447 = {
				add_manpower = -10000
				EGY_corvee_random_effects = yes
			}
			set_temp_variable = { suez_step = 2 }
			EGY_suez_begin_work = yes
			custom_effect_tooltip = EGY_suez_construct_2
		}

		remove_effect = {
			set_temp_variable = { suez_step = 2 }
			EGY_suez_finish_work = yes
			447 = {
				add_manpower = 9000
			}
		}

		cancel_effect = {
			set_temp_variable = { suez_step = 2 }
			EGY_suez_abort_work = yes
			447 = {
				add_manpower = 10000
			}
		}
	}

	EGY_corvee_workers_aswan = {
		icon = generic_construction
		fixed_random_seed = no

		allowed = { TAG = EGY }

		available = {
			check_variable = {
				var = EGY.suez_inprogress_total
				value = EGY.suez_progress_max
				compare = less_than
			}
			456 = {
				state_population > 9999
			}
			has_stability > 0.2
			NOT = { has_country_flag = EGY_corvee_suez_disabled }
		}

		visible = {
			NOT = { has_country_flag = EGY_corvee_suez_disabled }
			has_country_flag = suez_constructor_flag
			NOT = { has_global_flag = SUEZ_CANAL_BUILT }
			owns_state = 456
		}

		cost = 0
		ai_will_do = { factor = 100 }

		modifier = {
			political_power_gain = -0.02
			stability_factor = -0.025
		}

		days_remove = 240

		cancel_trigger = {
			check_variable = {
				var = EGY.suez_progress
				value = EGY.suez_progress_max
				compare = greater_than_or_equals
			}
		}

		complete_effect = {
			456 = {
				add_manpower = -10000
				EGY_corvee_random_effects = yes
			}
			set_temp_variable = { suez_step = 2 }
			EGY_suez_begin_work = yes
			custom_effect_tooltip = EGY_suez_construct_2
		}

		remove_effect = {
			set_temp_variable = { suez_step = 2 }
			EGY_suez_finish_work = yes
			456 = {
				add_manpower = 9000
			}
		}

		cancel_effect = {
			set_temp_variable = { suez_step = 2 }
			EGY_suez_abort_work = yes
			456 = {
				add_manpower = 10000
			}
		}
	}

	EGY_corvee_workers_cairo = {
		icon = generic_construction
		fixed_random_seed = no

		allowed = { TAG = EGY }

		available = {
			check_variable = {
				var = EGY.suez_inprogress_total
				value = EGY.suez_progress_max
				compare = less_than
			}
			446 = {
				state_population > 9999
			}
			has_stability > 0.2
			NOT = { has_country_flag = EGY_corvee_suez_disabled }
		}

		visible = {
			NOT = { has_country_flag = EGY_corvee_suez_disabled }
			has_country_flag = suez_constructor_flag
			NOT = { has_global_flag = SUEZ_CANAL_BUILT }
			owns_state = 446
		}

		cost = 0
		ai_will_do = { factor = 100 }

		modifier = {
			political_power_gain = -0.02
			stability_factor = -0.025
		}

		days_remove = 240

		cancel_trigger = {
			check_variable = {
				var = EGY.suez_progress
				value = EGY.suez_progress_max
				compare = greater_than_or_equals
			}
		}

		complete_effect = {
			446 = {
				add_manpower = -10000
				EGY_corvee_random_effects = yes
			}
			set_temp_variable = { suez_step = 2 }
			EGY_suez_begin_work = yes
			custom_effect_tooltip = EGY_suez_construct_2
		}

		remove_effect = {
			set_temp_variable = { suez_step = 2 }
			EGY_suez_finish_work = yes
			446 = {
				add_manpower = 9000
			}
		}

		cancel_effect = {
			set_temp_variable = { suez_step = 2 }
			EGY_suez_abort_work = yes
			446 = {
				add_manpower = 10000
			}
		}
	}

	#Dedicate Industrial Power
	EGY_dedicate_industrial_power = {
		icon = generic_construction

		allowed = { EGY_suez_participant_tags = yes }

		available = {
			num_of_civilian_factories_available_for_projects > 1
		}

		visible = {
			has_country_flag = suez_constructor_flag
			NOT = { has_global_flag = SUEZ_CANAL_BUILT }
			EGY = { exists = yes }
			check_variable = {
				var = EGY.suez_inprogress_total
				value = EGY.suez_progress_max
				compare = less_than
			}
		}

		custom_cost_trigger = {
			set_temp_variable = {
				amount_of_money_to_check = 1
			}
			has_specific_amount_of_money_with_inflation_included = yes
		}
		custom_cost_text = eoanb_money_cost_text

		days_remove = 240
		days_re_enable = 10

		cancel_trigger = {
			check_variable = {
				var = EGY.suez_progress
				value = EGY.suez_progress_max
				compare = greater_than_or_equals
			}
		}

		modifier = {
			civilian_factory_use = 2
		}

		complete_effect = {
			set_temp_variable = { money_to_gain = -1 }
			add_money_with_inflation_included_with_tooltip_effect = yes
			set_temp_variable = { suez_step = 2 }
			EGY_suez_begin_work = yes
			custom_effect_tooltip = EGY_suez_construct_2
		}

		remove_effect = {
			set_temp_variable = { suez_step = 2 }
			EGY_suez_finish_work = yes
		}

		cancel_effect = {
			set_temp_variable = { suez_step = 2 }
			EGY_suez_abort_work = yes
			set_temp_variable = { money_to_gain = 1 }
			add_money_with_inflation_included_with_tooltip_effect = yes
		}

		ai_will_do = {
			base = 100
		}
	}

	#Build Access Routes to the Suez Canal Sites
	# Deliberately a pre-construction decision: available only in the window
	# between afr_EGY_vic.23 and france.813, before Egypt flips from
	# contributor to constructor.
	EGY_prepare_the_site = {
		icon = generic_construction

		allowed = { tag = EGY }

		available = {
			has_manpower > 999
			owns_state = 749
		}

		visible = {
			NOT = { has_country_flag = suez_constructor_flag }
			has_country_flag = suez_contributor_flag
			NOT = { has_global_flag = SUEZ_CANAL_BUILT }
		}

		fire_only_once = yes

		custom_cost_trigger = {
			set_temp_variable = {
				amount_of_money_to_check = 1
			}
			has_specific_amount_of_money_with_inflation_included = yes
		}
		custom_cost_text = eoanb_money_cost_text

		days_remove = 100

		cancel_trigger = {
			check_variable = {
				var = EGY.suez_progress
				value = EGY.suez_progress_max
				compare = greater_than_or_equals
			}
		}

		complete_effect = {
			set_temp_variable = { money_to_gain = -1 }
			add_money_with_inflation_included_with_tooltip_effect = yes
			add_manpower = -1000
			set_temp_variable = { suez_step = 2 }
			EGY_suez_begin_work = yes
			custom_effect_tooltip = EGY_suez_construct_2
		}

		remove_effect = {
			set_temp_variable = { suez_step = 2 }
			EGY_suez_finish_work = yes
			add_manpower = 900
			if = {
				limit = { owns_state = 749 }
				749 = {
					add_building_construction = {
						type = infrastructure
						level = 1
						instant_build = yes
					}
				}
			}
		}

		cancel_effect = {
			set_temp_variable = { suez_step = 2 }
			EGY_suez_abort_work = yes
			add_manpower = 1000
			set_temp_variable = { money_to_gain = 1 }
			add_money_with_inflation_included_with_tooltip_effect = yes
		}

		ai_will_do = {
			base = 1000
		}
	}

	#Build Port Said (northern harbour)
	EGY_build_northern_harbor = {
		icon = naval_base

		allowed = { tag = EGY }

		available = {
			has_manpower > 3999
			controls_state = 749
			check_variable = {
				var = EGY.suez_inprogress_total
				value = EGY.suez_progress_max
				compare = less_than
			}
		}

		visible = {
			has_country_flag = suez_constructor_flag
			EGY = { has_variable = suez_progress }
			NOT = { has_global_flag = SUEZ_CANAL_BUILT }
			owns_state = 749
		}

		fire_only_once = yes

		custom_cost_trigger = {
			set_temp_variable = {
				amount_of_money_to_check = 2
			}
			has_specific_amount_of_money_with_inflation_included = yes
		}
		custom_cost_text = eoanb_money_cost_text

		days_remove = 480

		cancel_trigger = {
			check_variable = {
				var = EGY.suez_progress
				value = EGY.suez_progress_max
				compare = greater_than_or_equals
			}
		}

		complete_effect = {
			set_temp_variable = { money_to_gain = -2 }
			add_money_with_inflation_included_with_tooltip_effect = yes
			add_manpower = -4000
			set_temp_variable = { suez_step = 5 }
			EGY_suez_begin_work = yes
			custom_effect_tooltip = EGY_suez_construct_5
		}

		remove_effect = {
			set_temp_variable = { suez_step = 5 }
			EGY_suez_finish_work = yes
			add_manpower = 3600
			if = {
				limit = { owns_state = 749 }
				set_victory_points = {
					province = 12049
					value = 3
				}
				749 = {
					add_building_construction = {
						type = naval_base
						level = 2
						province = 12049
						instant_build = yes
					}
				}
				hidden_effect = {
					set_province_name = { id = 12049 name = "Port Said" }
				}
			}
		}

		cancel_effect = {
			set_temp_variable = { suez_step = 5 }
			EGY_suez_abort_work = yes
			add_manpower = 4000
			set_temp_variable = { money_to_gain = 2 }
			add_money_with_inflation_included_with_tooltip_effect = yes
		}

		ai_will_do = {
			base = 1000
		}
	}

	#Build Port Tawfiq (southern harbour)
	EGY_build_southern_harbor = {
		icon = naval_base

		allowed = { tag = EGY }

		available = {
			has_manpower > 3999
			controls_state = 749
			check_variable = {
				var = EGY.suez_inprogress_total
				value = EGY.suez_progress_max
				compare = less_than
			}
		}

		visible = {
			has_country_flag = suez_constructor_flag
			EGY = { has_variable = suez_progress }
			NOT = { has_global_flag = SUEZ_CANAL_BUILT }
			owns_state = 749
		}

		fire_only_once = yes

		custom_cost_trigger = {
			set_temp_variable = {
				amount_of_money_to_check = 1.5
			}
			has_specific_amount_of_money_with_inflation_included = yes
		}
		custom_cost_text = eoanb_money_cost_text

		days_remove = 480

		cancel_trigger = {
			check_variable = {
				var = EGY.suez_progress
				value = EGY.suez_progress_max
				compare = greater_than_or_equals
			}
		}

		complete_effect = {
			set_temp_variable = { money_to_gain = -1.5 }
			add_money_with_inflation_included_with_tooltip_effect = yes
			add_manpower = -4000
			set_temp_variable = { suez_step = 5 }
			EGY_suez_begin_work = yes
			custom_effect_tooltip = EGY_suez_construct_5
		}

		remove_effect = {
			set_temp_variable = { suez_step = 5 }
			EGY_suez_finish_work = yes
			add_manpower = 3600
			if = {
				limit = { owns_state = 749 }
				set_victory_points = {
					province = 4073
					value = 2
				}
				749 = {
					add_building_construction = {
						type = naval_base
						level = 1
						province = 4073
						instant_build = yes
					}
				}
				hidden_effect = {
					set_province_name = { id = 4073 name = "Port Tawfiq" }
				}
			}
		}

		cancel_effect = {
			set_temp_variable = { suez_step = 5 }
			EGY_suez_abort_work = yes
			add_manpower = 4000
			set_temp_variable = { money_to_gain = 1.5 }
			add_money_with_inflation_included_with_tooltip_effect = yes
		}

		ai_will_do = {
			base = 1000
		}
	}

	#Declare the Suez Canal Open
	EGY_open_the_canal = {
		icon = canal

		allowed = { EGY_suez_participant_tags = yes }

		available = {
			controls_state = 749
			custom_trigger_tooltip = {
				tooltip = suez_canal_completed_trigger_tt
				check_variable = {
					var = EGY.suez_progress
					value = EGY.suez_progress_max
					compare = greater_than_or_equals
				}
			}
		}

		visible = {
			NOT = { has_global_flag = SUEZ_CANAL_BUILT }
			owns_state = 749
			OR = {
				has_country_flag = suez_constructor_flag
				check_variable = {
					var = EGY.suez_progress
					value = EGY.suez_progress_max
					compare = greater_than_or_equals
				}
			}
		}

		fire_only_once = yes

		cost = 0

		complete_effect = {
			set_global_flag = SUEZ_CANAL_BUILT
			news_event = { id = afr_EGY_vic.20 days = 3 }
		}

		ai_will_do = {
			base = 1000
		}
	}

	# Kicks off the whole chain.
	FRA_suez_processing_of_the_report = {
		icon = GFX_decision_administration

		allowed = { tag = FRA }

		available = {
			EGY = { exists = yes }
			NOT = { has_war_with = EGY }
		}

		visible = {
			EGY = { has_variable = suez_progress }
			NOT = { has_global_flag = SUEZ_CANAL_BUILT }
			NOT = { has_country_flag = suez_constructor_flag }
		}

		fire_only_once = yes
		cost = 0
		days_remove = 582

		cancel_trigger = {
			OR = {
				NOT = { EGY = { exists = yes } }
				has_global_flag = SUEZ_CANAL_BUILT
			}
		}

		remove_effect = {
			country_event = { id = afr_EGY_vic.22 days = 1 }
		}

		ai_will_do = {
			base = 1000
		}
	}

	EGY_block_the_canal = {
		icon = generic_operation

		available = {
			controls_state = 749
			NOT = { has_global_flag = SUEZ_CANAL_BLOCKED }
		}

		visible = {
			owns_state = 749
			has_global_flag = SUEZ_CANAL_BUILT
			NOT = { has_global_flag = SUEZ_CANAL_BLOCKED }
		}

		cost = 10
		ai_will_do = {
			factor = 0
		}
		days_remove = 240

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 749 }
				has_global_flag = SUEZ_CANAL_BLOCKED
			}
		}

		remove_effect = {
			set_global_flag = SUEZ_CANAL_BLOCKED
		}
	}

	EGY_un_block_the_canal = {
		icon = canal

		available = {
			controls_state = 749
			has_global_flag = SUEZ_CANAL_BLOCKED
		}

		visible = {
			owns_state = 749
			has_global_flag = SUEZ_CANAL_BUILT
			has_global_flag = SUEZ_CANAL_BLOCKED
		}

		cost = 10
		ai_will_do = {
			factor = 0
		}
		days_remove = 240

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 749 }
				NOT = { has_global_flag = SUEZ_CANAL_BLOCKED }
			}
		}

		remove_effect = {
			clr_global_flag = SUEZ_CANAL_BLOCKED
		}

		modifier = {
			civilian_factory_use = 2
		}
	}

	EGY_invite_ETH_to_invest = {
		icon = generic_operation

		allowed = { TAG = EGY }

		available = {
			has_country_flag = suez_constructor_flag
			ETH = { exists = yes }
			NOT = { has_war_with = ETH }
		}

		visible = {
			has_country_flag = ETH_EGY_alliance
			ETH = { exists = yes }
			NOT = { has_global_flag = SUEZ_CANAL_BUILT }
			NOT = {
				is_in_array = {
					array = EGY.suezInvestors
					value = ETH.id
				}
			}
			check_variable = {
				var = EGY.suez_inprogress_total
				value = EGY.suez_progress_max
				compare = less_than
			}
		}

		cost = 10
		days_re_enable = 730
		ai_will_do = { factor = 100 }

		complete_effect = {
			ETH = { country_event = { id = afr_EGY_vic.26 } }
		}
	}

}

eoanb_category_diplomatic_actions = {

	EGY_ETH_war_for_gedaref = {
		icon = generic_political_actions

		is_good = yes
		days_mission_timeout = 150

		allowed = { tag = EGY }
		activation = { always = no } # activated by EGY_vic_seize_the_gedaref

		cancel_trigger = {
			OR = {
				NOT = { ETH = { exists = yes } }
				NOT = { has_war_with = ETH }
			}
		}

		available = {
			controls_state = 817
			has_war_with = ETH
		}

		complete_effect = {
			if = {
				limit = { has_war_with = ETH }
				white_peace = ETH
			}
			transfer_state = 817
		}

		timeout_effect = {
			if = {
				limit = { has_war_with = ETH }
				white_peace = ETH
			}
			add_political_power = -50
		}
	}

}
