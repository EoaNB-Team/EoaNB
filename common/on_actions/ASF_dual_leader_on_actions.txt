on_actions = {
	on_startup = {
		effect = {
			# DUAL LEADER SET UP INFO
			ENG = {
				set_temp_variable = { temp_char = token:ENG_Victoria }
				ASF_character_data = yes
				ENG_Victoria = { ASF_promote_character_to_dual_leader = yes }
				set_temp_variable = { temp_title = token:VL_Queen }
				set_temp_variable = { temp_position = 1 } #NEEDS TO BE CUT OUT
				ASF_change_leader_title = yes
			}
		}
	}
	on_weekly = {
		effect = {
			add_to_variable = { ASF_dual_leader_dirty = 1 }
		}
	}
}