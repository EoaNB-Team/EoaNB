#	Ordered in alphabetical order by their respective tags
#
#			# Add more countries here following this pattern:
#			TAG = {
#				set_variable = { VL_status_var = X }  # 1 = Generic, 2 = Emperor, 3 = King, 4 = Queen, 5 = Vice President, 6 = Prime Minister, 7 = Chancellor, 8 = Grand Duke, 9 = Grand Vizier
#				add_ideas = TAG_VL_NAME
#			}

on_actions = {
	on_startup = {
		effect = {
			# Initialize Vice Leaders on game start

			# (AUS) Austria - Emperor Franz Joseph I (status 2 = Emperor)
			AUS = {
				set_variable = { VL_status_var = 2 }
				add_ideas = AUS_VL_Franz_Joseph_I
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			# (BAD) Baden - Grand Duke Friedrich I (status 8 = Grand Duke)
			BAD = {
				set_variable = { VL_status_var = 8 }
				add_ideas = BAD_VL_Friedrich_I
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			# (BEL) Belgium - King Leopold I (status 3 = King)
			BEL = {
				set_variable = { VL_status_var = 3 }
				add_ideas = BEL_VL_Leopold_I
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			# (BRA) Brazil - Emperor Pedro II (status 2 = Emperor)
			BRA = {
				set_variable = { VL_status_var = 2 }
				add_ideas = BRA_VL_Pedro_II
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			# (DEN) Denmark - King Frederick VII (status 3 = King)
			DEN = {
				set_variable = { VL_status_var = 3 }
				add_ideas = DEN_VL_Frederick_VII
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			# (ENG) United Kingdom - Queen Victoria (status 4 = Queen)
			ENG = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			BCO = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			BRK = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			CAN = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			HBC = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			NAT = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			NFL = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			NSW = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			NVS = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			NZA = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			NZL = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			QNL = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			RAJ = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			SAF = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			SAS = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			SRC = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			TSM = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			VAN = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			VIC = {
				set_variable = { VL_status_var = 4 }
				add_ideas = ENG_VL_Victoria
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			# (GRE) Greece - King Otto (status 3 = King)
			GRE = {
				set_variable = { VL_status_var = 3 }
				add_ideas = GRE_VL_Otto
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			# (HOL) Netherlands - King William III (status 3 = King)
			HOL = {
				set_variable = { VL_status_var = 3 }
				add_ideas = HOL_VL_William_III
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			# (LUX) Luxembourg - King William III (status 3 = King)
			LUX = {
				set_variable = { VL_status_var = 3 }
				add_ideas = LUX_VL_William_III
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			# (NOR) Norway - King Oscar I (status 3 = King)
			NOR = {
				set_variable = { VL_status_var = 3 }
				add_ideas = NOR_VL_Oscar_I
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			# (OLD) Oldenburg - Grand Duke Peter II (status 8 = Grand Duke)
			OLD = {
				set_variable = { VL_status_var = 8 }
				add_ideas = OLD_VL_Peter_II
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}
			
			# (PER) Domains of Iran - Grand Vizier Mirza Aqa Khan Nuri (status 9 = Grand Vizier)
			PER = {
				set_variable = { VL_status_var = 9 }
				add_ideas = PER_VL_aqa_khan_nuri
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}
			
			# (PIE) Sardinia-Piedmont - King Victor Emmanuel II (status 3 = King)
			PIE = {
				set_variable = { VL_status_var = 3 }
				add_ideas = PIE_VL_Victor_Emmanuel_II
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			# (SWE) Sweden - King Oscar I (status 3 = King)
			SWE = {
				set_variable = { VL_status_var = 3 }
				add_ideas = SWE_VL_Oscar_I
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			# (TSH) Tokugawa Shogunate - Emperor Komei (status 2 = Emperor)
			TSH = {
				set_variable = { VL_status_var = 2 }
				add_ideas = TSH_VL_Komei
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}

			# (USA) United States of America - Vice President Breckenridge (status 5 = Vice President)
			USA = {
				set_variable = { VL_status_var = 5 }
				add_ideas = USA_VL_Breckinridge
				# Uncomment below to show vice leader by default instead of main leader
				# set_country_flag = show_vice_leader_window
			}
		}
	}
}
