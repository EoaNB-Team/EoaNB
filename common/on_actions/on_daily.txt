on_actions = {
    on_daily_HEN = {
		effect = {
			if = {
				limit = { has_war_with = FRA controls_state = 814 }
				FRA_henri_victory_effect = yes
			}
		}
	}
	on_daily_FRA = {
		effect = {
			if = {
				limit = {
					has_country_flag = FRA_henri_revolt_underway
					NOT = { exists = HEN }
				}
				clr_country_flag = FRA_henri_revolt_underway
				clear_variable = FRA_royal_support
				clear_variable = FRA_royal_support_weekly
			}
		}
	}
}