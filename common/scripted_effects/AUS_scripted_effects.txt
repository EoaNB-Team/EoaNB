# Austria Multinational Empire
	# Propose Decision - Will allow you to enable/setup a decision for russia's unique reform GUI
	# # set_temp_variable = { AUS_decision_token = token:<decision_token> } # token of decision
	# # set_temp_variable = { AUS_decision_lp_cost = <lp_cost> } # country must have and will spend this lp
	# # set_temp_variable = { AUS_decision_category = <category_num> } # what tab it will appear on
	# #	set_temp_variable = { AUS_decision_icon = <gfx_num_value> } # what icon the decision will recieve (GetRussianPeasantIcon) in scripted loc
	
	AUS_multinational_empire_propose_decision = { 
		custom_effect_tooltip = AUS_multinational_empire_propose_decision_tt
		set_variable = { lp_cost@var:AUS_decision_token = AUS_decision_lp_cost }
		set_variable = { icon@var:AUS_decision_token = AUS_decision_icon }
		if = {
			limit = { check_variable = { aus_decision_category = 1 } }
			add_to_array = {
				array = AUS_decision_array_1
				value = AUS_decision_token
			}
		}
	}
	
	# # set_temp_variable = { v = token:<decision_token> } # this is automatic when scoped to an array
	AUS_multinational_empire_enact_decision = {
		# custom_effect_tooltip = AUS_multinational_empire_enact_decision_tt

		custom_effect_tooltip = AUS_multinational_empire_enact_decision_lp_cost_tt
		set_temp_variable = { lp_cost = lp_cost@var:v }
		multiply_temp_variable = { lp_cost = -1 }
		log = "[?lp_cost]"
		add_political_power = var:lp_cost

		if = {
		}

		set_variable = { clicked@var:v = 1 }

		#### DECISION EFFECTS

		if = {
			limit = { check_variable = { v = token:AUS_decision_test_1 } }
			activate_decision = AUS_decision_test_1
		}

		if = {
			limit = { check_variable = { v = token:AUS_decision_test_2 } }
			activate_decision = AUS_decision_test_2
		}
	}




