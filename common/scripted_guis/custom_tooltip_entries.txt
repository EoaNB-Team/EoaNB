scripted_gui = {
	custom_tooltip_entries = {
		window_name = "custom_tooltip_entries"
		parent_window_token = top_bar

		context_type = player_context

		visible = {
		}

		properties = {
		}

		effects = {
		}

		dynamic_lists = {
		}

		triggers = {
		}

		ai_enabled = {
			always = no
		}
	}
}