scripted_gui = {
	napoleon_health_decision_ui = {
		context_type = decision_category
		window_name = "napoleon_health_decision_ui_window"

		properties = {
			napoleon_healthy_progress_bar = {
				frame = FRA.napoleon_health
			}
		}

		triggers = {
			napoleon_bust_1_visible = {
				check_variable = {
    				var = FRA.napoleon_health
    				value = 75
    				compare = less_than_or_equals
				}
			}

			napoleon_bust_2_visible = {
				OR = {
					check_variable = {
    					var = FRA.napoleon_health
    					value = 50
    					compare = less_than
					}
					check_variable = {
    					var = FRA.napoleon_health
    					value = 75
    					compare = greater_than_or_equals
					}
				}
			}

			napoleon_bust_3_visible = {
				OR = {
					check_variable = {
    					var = FRA.napoleon_health
    					value = 25
    					compare = less_than
					}
					check_variable = {
    					var = FRA.napoleon_health
    					value = 50
    					compare = greater_than_or_equals
					}
				}
			}

			napoleon_bust_4_visible = {
				OR = {
					check_variable = {
    					var = FRA.napoleon_health
    					value = 0
    					compare = less_than
					}
					check_variable = {
    					var = FRA.napoleon_health
    					value = 25
    					compare = greater_than_or_equals
					}
				}
			}

			napoleon_bust_1_current_stage_visible = {
				check_variable = {
    				var = FRA.napoleon_health
    				value = 75
    				compare = greater_than_or_equals
				}
			}

			napoleon_bust_2_current_stage_visible = {
				AND = {
					check_variable = {
    					var = FRA.napoleon_health
    					value = 50
    					compare = greater_than_or_equals
					}
					check_variable = {
    					var = FRA.napoleon_health
    					value = 75
    					compare = less_than
					}
				}
			}

			napoleon_bust_3_current_stage_visible = {
				AND = {
					check_variable = {
    					var = FRA.napoleon_health
    					value = 25
    					compare = greater_than_or_equals
					}
					check_variable = {
    					var = FRA.napoleon_health
    					value = 50
    					compare = less_than
					}
				}
			}

			napoleon_bust_4_current_stage_visible = {
				AND = {
					check_variable = {
    					var = FRA.napoleon_health
    					value = 0
    					compare = greater_than_or_equals
					}
					check_variable = {
    					var = FRA.napoleon_health
    					value = 25
    					compare = less_than
					}
				}
			}

			fra_napoleon_text_all_is_well_visible = {
				check_variable = {
    				var = FRA.napoleon_health
    				value = 75
    				compare = greater_than_or_equals
				}
			}

			fra_napoleon_text_improving_situation_visible = {
				AND = {
					check_variable = {
    					var = FRA.napoleon_health
    					value = 50
    					compare = greater_than_or_equals
					}
					check_variable = {
    					var = FRA.napoleon_health
    					value = 75
    					compare = less_than
					}
				}
			}

			fra_napoleon_text_serious_illness_visible = {
				AND = {
					check_variable = {
    					var = FRA.napoleon_health
    					value = 25
    					compare = greater_than_or_equals
					}
					check_variable = {
    					var = FRA.napoleon_health
    					value = 50
    					compare = less_than
					}
				}
			}

			fra_napoleon_text_impending_coma_visible = {
				AND = {
					check_variable = {
    					var = FRA.napoleon_health
    					value = 0
    					compare = greater_than_or_equals
					}
					check_variable = {
    					var = FRA.napoleon_health
    					value = 25
    					compare = less_than
					}
				}
			}
		}
	}
}