country_and_all_subjects = {
	input = game:scope
	operators =  { country_and_all_subjects }
	name = COLLECTION_COUNTRY_AND_ALL_SUBJECTS
}

every_owned_cored_state = {
	input = game:scope
	operators = { 
		owned_states 
		limit = { is_core_of = PREV }
	}
	name = COLLECTION_EVERY_OWNED_AND_CORED_STATE
}

every_owned_coreless_state = {
	input = game:scope
	operators = { 
		owned_states 
		limit = { NOT = { is_core_of = PREV } }
	}
	name = COLLECTION_EVERY_OWNED_AND_CORELESS_STATE
}