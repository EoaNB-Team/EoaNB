weekly_manpower = {
  base = 5
  modifier = {
    set_temp_variable = {
      var = weekly_manpower_temp
      value = 0
    }
    all_owned_state = {
      add_to_temp_variable = {
        var = weekly_manpower_temp
        value = mtth:local_weekly_manpower
      }
    }
  }
  modifier = {
    add = modifier@weekly_manpower
  }
  modifier = {
    set_temp_variable = {
      var = weekly_manpower_factor_temp
      value = 1
    }
    add_to_temp_variable = {
      var = weekly_manpower_factor_temp
      value = modifier@weekly_manpower_factor
    }
    factor = weekly_manpower_factor_temp
  }
}
local_weekly_manpower = {
  base = 1
  modifier = {
    add = modifier@local_weekly_manpower
  }
  modifier = {
    set_temp_variable = {
      var = local_weekly_manpower_factor_temp
      value = 1
    }
    add_to_temp_variable = {
      var = local_weekly_manpower_factor_temp
      value = modifier@local_weekly_manpower_factor
    }
    factor = local_weekly_manpower_factor_temp
  }
}