GER_CONF_defensive_war_effect = {
    #every country with the German Confederation idea declares war on the attacker (ROOT)
    every_country = {
        limit = {
            has_idea = GER_general_idea_ger_confed
        }
        declare_war_on = {
			target = ROOT
			type = wargoal_diplomatic_humiliation
		}
    }
}